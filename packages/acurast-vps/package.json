{
  "name": "@acurast/vps",
  "version": "0.1.0",
  "description": "VPS deployment helpers for Acurast — keypair generation, domain precomputation, job spec building",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@acurast/sdk": "^1.3.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
